

@implements IDisposable
@inherits LayoutComponentBase
@inject TreeStateService TreeStateService
@inject ArticleApiService ArticleApiService
@using Chronicis.Client.Themes
@using Chronicis.Client.ViewModels

<MudThemeProvider Theme="@_chronicisMudTheme" />
<MudDialogProvider />
<MudSnackbarProvider />
<MudPopoverProvider />

<MudLayout>
    <MudAppBar Elevation="1" Dense="true" Class="chronicis-appbar">
        <MudIconButton Icon="@Icons.Material.Filled.Menu"
                       Color="Color.Inherit"
                       Edge="Edge.Start"
                       OnClick="@ToggleDrawer" />
            <MudImage Src="/images/logo.png" Height="32" Class="mr-2" />
            <MudText Typo="Typo.h6" Class="chronicis-heading">CHRONICIS</MudText>
        <MudSpacer />
        <MudIconButton Icon="@Icons.Material.Filled.AccountCircle" Color="Color.Inherit" />
        <MudIconButton Icon="@Icons.Material.Filled.Settings" Color="Color.Inherit" />
    </MudAppBar>

    <MudDrawer @bind-Open="_drawerOpen"
               Variant="@DrawerVariant.Persistent"
               Elevation="0"
               Fixed="true"
               ClipMode=DrawerClipMode.Docked
               Width="320px">
        <MudDrawerHeader>
            <SearchBox />
        </MudDrawerHeader>
        <ArticleTreeView TreeItems="@_treeItems" 
            OnArticleSelected="@OnArticleSelected" />
    </MudDrawer>

    <MudMainContent>
        <MudContainer MaxWidth="MaxWidth.Large" Class="mt-4">
            @Body
        </MudContainer>
    </MudMainContent>
</MudLayout>

@code {
    private bool _drawerOpen = true;
    private string _searchQuery = string.Empty;
    private List<ArticleTreeItemViewModel> _treeItems = new();
    private MudTheme _chronicisMudTheme = new ChronicisMudTheme();

    protected override void OnInitialized()
    {
        TreeStateService.OnStateChanged += UpdateTree;
        _treeItems = TreeStateService.RootItems;
    }

    private void UpdateTree()
    {
        _treeItems = TreeStateService.RootItems;
        StateHasChanged();
    }

    public void Dispose()
    {
        TreeStateService.OnStateChanged -= UpdateTree;
    }

    private void ToggleDrawer()
    {
        _drawerOpen = !_drawerOpen;
    }

    private void OnSearchKeyUp(KeyboardEventArgs e)
    {
        if (e.Key == "Enter")
        {
            // Implement search in Phase 3
        }
    }

    private void OnArticleSelected(ArticleTreeItemViewModel item)
    {
        TreeStateService.SelectArticle(item);
    }
}
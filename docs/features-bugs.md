Feature ideas/bugs:

Navbar:
    Drag and drop in the navbar to reorder articles
    Add sections in navbar to other functionality
    Fix navbar alignment when a parent and child are peers

UI Improvements:
    Add a footer to the page for links like About, Contact, and Privacy Policy
    Force article titles to be unique

Article Detail:
    Clicking on the Home breadcrumb does not return to the home page
    All the steps in the breadcrumbs are not showing up
    Set article type as "generated" - it searches all pages that link to the page and then build an AI summary and displays under the content box.

Hashtags
    When I create one hashtag and then hit space to render it, then I type in another hashtag, it doesn't style properly. But if I then type in a third it does. This continues, every other one...